import requests
from bs4 import BeautifulSoup as bs


class Citilink:
    def __init__(self, url, name):
        pass

# r = requests.get(url)
# s = bs(r.text, 'html.parser')
# price = s.find_all('span', class_='ProductPrice__price ProductPagePriceSection__default-price__price')
# price = (str(price).split('\n')[1])
# print(price.strip())
